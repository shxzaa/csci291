#include <stdio.h>

// Define constants
#define ESPRESSO_BEANS 8
#define ESPRESSO_WATER 30
#define ESPRESSO_PRICE 3.5

#define CAPPUCCINO_BEANS 8
#define CAPPUCCINO_WATER 30
#define CAPPUCCINO_MILK 70
#define CAPPUCCINO_PRICE 4.5

#define MOCHA_BEANS 8
#define MOCHA_WATER 39
#define MOCHA_MILK 160
#define MOCHA_SYRUP 30
#define MOCHA_PRICE 5.5

#define LOW_BEANS 10
#define LOW_WATER 50
#define LOW_MILK 100
#define LOW_SYRUP 20

#define ADMIN_PASSWORD 1234

// Ingredient quantities in the machine
float beans = 100;
float water = 500;
float milk = 300;
float syrup = 100;
float total_amount = 0.0; // Total sales

// Function to check if coffee can be served based on available ingredients
void displayMenu() {
    printf("Coffee Menu:\n");
    
    // Check if Espresso can be served
    if (beans >= ESPRESSO_BEANS && water >= ESPRESSO_WATER) {
        printf("%-12s AED %.2f\n", "1. Espresso", ESPRESSO_PRICE);
    } else {
        printf("%-12s %s\n", "1. Espresso", "Unavailable due to insufficient ingredients");
    }
}
